{
  "name": "tcm-arts",
  "version": "1.0.0",
  "description": "The Common Mass Arts (TCM Arts) - A Full Stack Web App for Art, Skating, and Chess",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"npm start --prefix backend\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.1",
    "@vuer-ai/react-helmet-async": "^2.0.7",
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "terser": "^5.48.0",
    "vite": "^8.0.16",
    "vite-plugin-seo-files": "^2.1.1"
  }
}
